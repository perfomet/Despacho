@model Datos.Modelo.Prioridad
@{
	ViewBag.SeccionTieneTitulo = true;
	ViewBag.Pagina = 13;

	if (Model.prioridadid == 0)
	{
		ViewBag.Title = "Despacho - Nueva Prioridad";
		ViewBag.TituloSeccion = "Nueva Prioridad";
	}
	else
	{
		ViewBag.Title = "Despacho - " + Model.descripcion;
		ViewBag.TituloSeccion = Model.descripcion;
	}
}

@section Less{
	<link rel="stylesheet/less" type="text/css" href="~/Content/less/prioridad.less" />
}

@section Scripts{
	@Scripts.Render("~/bundles/prioridad")
}

<div class="m-portlet m-portlet--tab">
	<!--begin::Form-->
	<form class="m-form m-form--fit m-form--label-align-right">
		<div class="m-portlet__body">
			<input type="hidden" value="@Model.prioridadid" id="id">
			<input type="hidden" value="@Model.EstaActivo" id="activo">

			<div class="form-group m-form__group row">
				<label for="example-text-input" class="col-2 col-form-label">Descripción</label>
				<div class="col-10">
					<input class="form-control m-input" type="text" value="@Model.descripcion" id="descripcion" style="width: 50%">
				</div>
			</div>

		</div>
		<div class="m-portlet__foot m-portlet__foot--fit">
			<div class="m-form__actions">
				<div class="row">
					<div class="col-2">
					</div>
					<div class="col-10">
						<button type="button" class="btn btn-success" id="btnGuardar">Guardar</button>
						<a href="/Prioridad/Index" class="btn btn-secondary">Volver</a>
					</div>
				</div>
			</div>
		</div>
	</form>
</div>





